import 'package:flutter/material.dart';

ThemeData buildThemeData() {
  // final baseTheme = ThemeData(fontFamily: fontPrimary);
  final baseTheme = ThemeData();

  return baseTheme.copyWith(
    primaryColor: Color(0xFF5460FE),
  );
}
